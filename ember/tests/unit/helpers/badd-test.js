import { badd } from 'trixionary/helpers/badd';
import { module, test } from 'qunit';

module('Unit | Helper | badd');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = badd([42]);
  assert.ok(result);
});
